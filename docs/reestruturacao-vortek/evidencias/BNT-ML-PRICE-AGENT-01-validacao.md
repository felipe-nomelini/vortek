# Canal técnico de preço ML — validação produtiva de 17/09/2026

- `dev` e `bentevi-prod` receberam `e3567abcd2701f4d8babcf12f042de3b1130eece` por fast-forward. O Easypanel concluiu o deploy `cmu5kpyky005v07ov3cl64z0e` no serviço `local/bentevi-prod`; o SHA em execução foi conferido pela API do painel. `app.bentevi.shop/api/ops/health` respondeu 200 e a nova rota recusou chamada sem assinatura com 401.
- `npm run validate`, `npm run build`, `npm run check:build-secrets` e os testes `pricing-decisions`, `ml-agent-price-auth` e `ml-agent-price-route` passaram. Não houve migration nesta ação; o banco produtivo foi comprovado em `192.168.1.162`, com migrations até `20260917003000`, funções auditadas `enqueue_manual_ml_command` e `claim_manual_ml_dispatch`, e colunas necessárias em `profiles`.
- Criada identidade Auth técnica com marcador exclusivo, sem senha e com login bloqueado; perfil `admin` exigido pela função de preço. A lista de usuários da aplicação oculta esse marcador. Segredo e ID do ator foram instalados somente no ambiente privado do Easypanel e no `.env.local` ignorado. A configuração anterior do serviço foi guardada em backup privado local antes da atualização; nenhum valor de credencial foi versionado.
- **Giannini, kit de dois jogos de cordas**, produto `2cb000e7-a947-4433-b1a8-092d550169b9`, anúncio `MLB7294124560`: cotação viva de R$ 120,00 para R$ 119,00, lucro unitário projetado de R$ 6,17 para R$ 7,56. Operação `3224a01e-5bf8-4ed2-908a-736b3763279d` confirmada, outbox concluída e leitura autenticada no ML mostrou R$ 119,00, status `active`, seller `3294514937`.
- **Rayovac, 40 pilhas AAA**, produto `af5941dc-8ae1-48bd-8107-8761489202d6`: anúncio comum `MLB7111661936` cotou R$ 82,66 para R$ 75,99 com lucro de R$ 8,27 para R$ 8,88. O catálogo `MLB4880564265` foi bloqueado por `IDENTIDADE_ANUNCIO_PENDENTE` antes da operação. A ficha local diz “tubo com 40 unidades”; o catálogo diz “10 cartelas com 4 unidades”, com `UNITS_PER_PACK=4` e `PACKS_NUMBER=10`; o anúncio comum tem `UNITS_PER_PACK=40`. Como o plano condicionou o par à validação conjunta, **nenhuma alteração Rayovac foi enviada**. Leitura autenticada final confirmou ambos ativos a R$ 82,66 e nenhuma operação do agente para eles.

Recuperação do canal: restaurar o ambiente privado do Easypanel a partir do backup local, implantar o serviço e remover a identidade técnica e seu perfil após verificar que não há operação pendente. Preço já confirmado no ML exige nova cotação e operação corretiva; restauração de banco não substitui essa conferência.
